# Translator własnego języka do C

## Użycie
Przy użyciu cargo
```bash
cargo run [input_file] [output_file]
```
lub od razu za pomocą pliku binarnego
```bash
./miac_lang [input_file] [output_file]
```
