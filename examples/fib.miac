

fn fib(x: i32) -> i32 {
    if x == 0 || x == 1 {
        return 1;
    }

    return fib(x - 1) + fib(x - 2);
}

fn main() -> i32 {
    const x: i32 = 5;
    var a: i32 = fib(x);

    const q: i32 = printf("fib %d: %d\n", x, a);

    return 0;
}
